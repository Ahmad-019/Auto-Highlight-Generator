# Project Deployment

## Hosting
The application is deployed on **Render** (or Streamlit Cloud), enabling public access to the BI tool.

## Requirements Management
Project dependencies are managed through the `requirements.txt` file, ensuring all libraries like `torch` and `transformers` are correctly installed in the production environment.
